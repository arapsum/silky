services:
  database:
    image: postgres:18.3-alpine
    container_name: silk-database
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    networks:
      - postgresql

networks:
  postgresql:
